In this example the specification is taken from external YAML file
---
tags:
  -  poem
parameters:
  - name: 诗词查询-根据id
    in: body
    schema:
      required:
        - id
      properties:
        id:
          type: string
          description: 用户名
responses:
   200:
      description: 响应结果正确
   400:
      description: 响应结果错误