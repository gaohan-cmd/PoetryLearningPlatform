用户删除-根据id
---
tags:
  -  table
parameters:
  - name: body
    in: body
    schema:
      required:
        - id
      properties:
        id:
          type: string
          description: 用户名
responses:
   200:
      description: 响应结果正确
   400:
      description: 响应结果错误