用户添加
---
tags:
  -  table
parameters:
  - name: body
    in: body
    schema:
      required:
        - username
        - password
        - email
        - role
      properties:
        username:
          type: string
          description: 用户名
        password:
          type: string
          description: 密码
        email:
          type: string
          description: 邮箱
        role:
          type: string
          description: 角色
responses:
   200:
      description: 响应结果正确
   400:
      description: 响应结果错误