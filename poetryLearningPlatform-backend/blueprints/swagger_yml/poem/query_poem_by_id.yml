诗词查询-根据id
---
tags:
  -  poem
parameters:
  - name: p_id
    in: query
    schema:
      required:
        - p_id
      properties:
        p_id:
          type: string
          description: 诗词id
responses:
   200:
      description: 响应结果正确
   400:
      description: 响应结果错误