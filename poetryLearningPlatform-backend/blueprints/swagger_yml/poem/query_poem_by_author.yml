诗词查询-根据作者id
---
tags:
  -  poem
parameters:
  - name: a_id
    in: query
    schema:
      required:
        - a_id
      properties:
        a_id:
          type: string
          description: 作者id
responses:
   200:
      description: 响应结果正确
   400:
      description: 响应结果错误