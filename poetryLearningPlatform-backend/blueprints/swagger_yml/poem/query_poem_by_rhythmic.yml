诗词查询-根据韵律id
---
tags:
  -  poem
parameters:
  - name: r_id
    in: query
    schema:
      required:
        - r_id
      properties:
        r_id:
          type: string
          description: 韵律id
responses:
   200:
      description: 响应结果正确
   400:
      description: 响应结果错误