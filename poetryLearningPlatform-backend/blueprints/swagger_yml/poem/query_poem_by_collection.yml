诗词查询-根据诗集id
---
tags:
  -  poem
parameters:
  - name: c_id
    in: query
    schema:
      required:
        - c_id
      properties:
        c_id:
          type: string
          description: 诗集id
responses:
   200:
      description: 响应结果正确
   400:
      description: 响应结果错误