playground图像生成
---
tags:
  -  chat
parameters:
  - name: body
    in: body
    schema:
      required:
        - poetryContent
      properties:
        poetryContent:
          type: string
          description: 诗词内容
responses:
   200:
      description: 响应结果正确
   400:
      description: 响应结果错误